package com.mycodedojo.kotlinbasics

fun main(){
    var age = 40
    // greater than >
    // lower than <
    // greater equal >=
    // lower equal <=
    // equal ==
   if(age >= 21){
       println("you can go into the bar")
   }else{
       println("you cannot enter the bar")
   }
}